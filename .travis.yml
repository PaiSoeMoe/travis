languauge: node_js
node_js:
  - 12.14.0
cache: npm

addons:
  firefox: latest

install:
  - npm ci

script:
  - npm install karma-firefox-launcher --save-dev
  - npm test
  - npm build --prod

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master