languauge: node_js
node_js:
  - 12.14.0
env:
  global:
    -MOZ_HEADLESS=1

  addons:
    firefox: latest

cache: npm


install:
  - npm ci

script:
  - npm run-script build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master